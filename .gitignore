/bin/
*.test
coverage.out
.env
.agent/
reasonix.toml
.reasonix-runs/
.DS_Store

clients/python/dist/
clients/python/.venv/
clients/python/**/__pycache__/

clients/web/node_modules/
clients/web/dist/
clients/web/dist-ts/
clients/web/tsconfig.tsbuildinfo
clients/web/test-results/
internal/dashboard/dist/*
!internal/dashboard/dist/index.html

# root-level compiled binaries (build artifacts, never committed)
/hookrail-admin
/hookrail-api
/hookrail-ctl
/hookrail-dashboard
/hookrail-worker

# local-only explainer doc (kept on disk, not committed)
docs/hookrail-explained.md

# build-process plan docs — kept on disk, never published (contain internal
# tooling references; the public repo must not carry them)
docs/superpowers/
