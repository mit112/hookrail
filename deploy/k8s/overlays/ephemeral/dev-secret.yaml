# DEV ONLY — k3d throwaway
---
apiVersion: v1
kind: Secret
metadata:
  name: hookrail-db
  namespace: hookrail
stringData:
  owner_password: "hookrail"
  app_dsn: "postgres://hookrail_app:dev-app-pw-001@hookrail-pg-rw:5432/hookrail?sslmode=disable"
  app_pw: "dev-app-pw-001"
  migrator_dsn: "postgres://hookrail:hookrail@hookrail-pg-rw:5432/hookrail?sslmode=disable"
---
apiVersion: v1
kind: Secret
metadata:
  name: hookrail-app
  namespace: hookrail
stringData:
  master_key: "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
---
apiVersion: v1
kind: Secret
metadata:
  name: hookrail-admin
  namespace: hookrail
stringData:
  token: "dev-admin-token-001"
---
apiVersion: v1
kind: Secret
metadata:
  name: hookrail-dashboard
  namespace: hookrail
stringData:
  password: "dev-dashboard-pw-001"
  session_key: "dev-session-key-0123456789abcdef0123"
---
# CNPG bootstrap.initdb.secret: owner role credentials. Username MUST equal the
# Cluster owner (hookrail) and password MUST match migrator_dsn so the migrator
# Job authenticates after CNPG provisions the role.
apiVersion: v1
kind: Secret
metadata:
  name: hookrail-db-owner
  namespace: hookrail
type: kubernetes.io/basic-auth
stringData:
  username: "hookrail"
  password: "hookrail"
